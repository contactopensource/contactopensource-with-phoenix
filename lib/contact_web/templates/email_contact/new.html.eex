<h1>New Email contact</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.email_contact_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.email_contact_path(@conn, :index) %></span>
