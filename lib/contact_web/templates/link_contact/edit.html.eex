<h1>Edit Link contact</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.link_contact_path(@conn, :update, @link_contact)) %>

<span><%= link "Back", to: Routes.link_contact_path(@conn, :index) %></span>
