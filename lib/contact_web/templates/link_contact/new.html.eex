<h1>New Link contact</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.link_contact_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.link_contact_path(@conn, :index) %></span>
